dist/
node_modules/

# Generated by `wails dev` / `wails generate module`. Unused: the contract with
# the Go side is written out by hand in src/bridge.d.ts, so it typechecks
# without the Wails CLI and a rename on the Go side fails the build.
wailsjs/

# Written by the Wails CLI to detect dependency changes.
package.json.md5
